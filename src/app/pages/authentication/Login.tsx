import { Button } from "antd";
import React from "react";

type Props = {};

const Login = (props: Props) => {
  return (
    <div>
      Login
      <Button />
    </div>
  );
};

export default Login;
